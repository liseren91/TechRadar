node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.output
.vinxi
todos.json
.eslintcache
.superpowers/
.env.*
# Templates with placeholders only (no values) are tracked.
!deploy/vps/.env.prod.example
*.key
public/data/*.local.json

# npm lockfile — this project installs with bun (bun.lock)
package-lock.json

# Local browser-automation output
.playwright-mcp/

# Paid Jev verdicts (src/server/utils/verdict-store.ts)
.cache/

# VPS deployment: host-side backups (deploy/vps)
/backups
