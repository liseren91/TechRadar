# Optional — analytics/instrumentation script injected in <head>
VITE_INSTRUMENTATION_SCRIPT_SRC=

# TypeSafe (Jev) key. The dashboard server uses it to categorize live feed
# items (without it they show as "Unclassified"); `generate:feed` requires it
# to tag trend topics. Server/CI only — never expose it to client code.
# TYPESAFE_API_KEY=

# Optional contact email for the MyMemory translation API (non-English feed
# items). Keyless use is capped at ~5k chars/day per IP; with an email ~50k.
# MYMEMORY_EMAIL=
# Where paid Jev verdicts persist across restarts (default .cache/jev-verdicts.json).
# JEV_CACHE_FILE=

# The radar's memory (SQLite): items, daily observations, cross-source links,
# discovered themes, predictions, source health and the usage ledger. Backed
# up daily next to it under backups/ (newest 7 kept). Default .cache/history.db.
# HISTORY_DB=
# Days of history kept for items no longer seen (default 365).
# HISTORY_RETAIN_DAYS=365
# Where the daily backup (VACUUM INTO, newest 7 kept) goes. Default: backups/
# next to the database, i.e. the same volume; point it at a host-mounted
# directory so losing the volume does not lose the backups.
# BACKUP_DIR=/backups
# Minutes between scheduled feed rebuilds (default 5). 0 turns the schedule
# off: rebuilds then happen only on requests, and daily maintenance never runs.
# FEED_SCHEDULE_MINUTES=5

# Operator token. When set, forcing a rebuild (dashboard operator panel) and
# the usage ledger / storage details of /api/health require it (Bearer). Set
# it on any internet-facing server.
# ADMIN_TOKEN=

# Optional webhooks, posted as {"text": "..."} (Slack, Mattermost, n8n, ...):
# the weekly "what changed" report (once per ISO week), with extra watch terms,
# and alerts when a source stops returning items or recovers.
# REPORT_WEBHOOK_URL=
# REPORT_WATCH=Mamba,GRPO
# Immediate alerts when a new work mentions a REPORT_WATCH term (default:
# REPORT_WEBHOOK_URL).
# WATCH_WEBHOOK_URL=
# ALERT_WEBHOOK_URL=
# Link appended to the weekly report text.
# PUBLIC_BASE_URL=https://radar.example.com

# Optional GitHub token (no scopes needed; server-side only). Raises GitHub's
# search limit from ~10 to ~30 requests/minute, so the feed runs every GitHub
# query with larger pages. Never exposed to the dashboard or the extension.
# GITHUB_TOKEN=
# Optional contact email for OpenAlex's faster "polite pool".
# OPENALEX_MAILTO=

# Only required for local `bun run generate:feed` (not needed for dev server)
# ANTHROPIC_API_KEY=

# Digest model for `bun run generate:feed`. One of: claude-haiku-4-5 (default,
# ~$1.30/month) or claude-sonnet-5 (~$6/month). See scripts/generate-feed/model.ts.
# DIGEST_MODEL=claude-haiku-4-5

# Only needed if ANTHROPIC_API_KEY is an org-level key rather than a
# workspace-scoped one. Org keys are rejected without this header.
# ANTHROPIC_WORKSPACE_ID=wrkspc_...

# The digest uses the Message Batches API (50% cheaper) by default. Set to 0 to
# force synchronous calls, e.g. when debugging.
# DIGEST_BATCH=0
# How long to wait for a batch before falling back to synchronous calls.
# DIGEST_BATCH_TIMEOUT_MIN=90
